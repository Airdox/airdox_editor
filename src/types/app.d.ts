/**
 * @license
 * Von Vite gesetzte Konstanten (siehe `define` in `vite.config.ts`).
 */

/**
 * Fassung aus `package.json`. Dieselbe Zahl steht im Namen der Windows-Datei
 * (`Airdox_intelligents_Editor-<version>-win-x64.exe`), in der Versionsressource
 * der EXE, in der Titelleiste und im Block `app` jeder Projektdatei.
 */
declare const __APP_VERSION__: string;
