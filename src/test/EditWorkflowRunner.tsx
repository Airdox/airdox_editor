/**
 * In-Browser Edit-Workflow-Selbsttest für airdox_SMART_Editor v0.4.3
 * ----------------------------------------------------------------
 * Diese Seite simuliert einen systematischen Durchlauf aller 8 Edit-Aktionen
 * und erstellt Beweis-Screenshots via "Save PNG" (im Test-Modal).
 *
 * Über die Tastenkombination Ctrl+Shift+T im App kann der Test geöffnet werden.
 * In der Test-Routine werden alle Aktionen (per direkter Handler-Aufrufe
 * auf den App-State) ausgeführt – das gibt uns zuverlässige, reproduzierbare
 * Ergebnisse unabhängig von Playwright-Verfügbarkeit.
 *
 * Die Routine ist NICHT im UI des normalen Nutzers sichtbar und wird nur
 * aktiviert, wenn `?selftest=1` in der URL steht.
 */

// This file is a placeholder/documentation stub; the actual test lives in
// tests/edit-workflow.test.mjs (Playwright) – see CHANGELOG_v0.4.3.md for
// execution instructions.
export {};
