# Offene Grundsatzentscheidungen 2026-09-12 – geprüft, gewichtet, sortiert

Stand: 2026-09-12, Branch `arena/01a0933a-airdox-editor`, HEAD `2a91d77`.
Diese Vorlage ersetzt keine Entscheidung, sie liefert die belastbare Grundlage.
Alle Zahlen sind **gemessen** (Kommandos in §5), nicht aus Notizen übernommen.

## 1. Ausgangslage – die drei Punkte, und was wirklich offen ist

| Punkt | Behauptung (vorher) | Messbarer Befund (heute) |
|---|---|---|
| PR #14 | „offen, wartet auf Merge“ | `OPEN`, `MERGEABLE` in `main`, letzter CI-Lauf `34679691832`: ubuntu-Tests ✓, windows-Tests ✓, Windows-Installer+Portable ✓ (Artefakt 270 886 380 B). Keine offenen Änderungen im Arbeitsbaum. |
| Versions-/Tag-Politik | „unklar, ob 0.4.20 oder v0.5.10“ | `package.json` hier = **0.4.20**, Tag `v0.5.10` = `ded9226` (2026-09-10) in **eigener, verwandtschaftsfreier Historie**. Tags insgesamt: `v0.4 … v0.4.14`, `v0.5.0`, `v0.5.2 … v0.5.10` (`v0.5.1` fehlt). |
| „26 Commits fehlen `main`“ | Zahl aus Sitzungsnotiz | **Falsch.** `origin/main` ist genau **1 Commit** und selbst ein Merge (`734c1be` = PR #13). Gegen `v0.5.10` fehlen **81 Commits (71 Nicht-Merge)**; umgekehrt enthält `v0.5.10` **10** Commits nicht, die hier sind. |

Wichtigste Topologie-Tatsache: `git merge-base origin/main v0.5.10` liefert **nichts** – die beiden Historien haben **keinen gemeinsamen Vorfahren**. `git merge-tree` auf `main`/`v0.5.10` antwortet mit `fatal: refusing to merge unrelated histories`. Ein „einfacher Merge der v0.5.x-Linie“ ist damit technisch nicht möglich; GitHub meldet für deren eigenen Pull-Request **#12 `CONFLICTING`** (36 Dateien, +6448/−269).

## 2. Bewertete Übernahme-Kandidaten

Gewichtung: **Risiko** = Gefahr bei Übernahme (niedrig/mittel/hoch), **Wichtigkeit** = Nutzen für den Nutzeralltag, **Durchführbarkeit** = Aufwand + Konfliktlage (getrocknet mit `git cherry-pick -n` in einem Wegwerf-Worktree). Jede Zeile ist statisch verifiziert; „fehlt“ heißt: im Baum von `v0.5.10` vorhanden, hier nicht auffindbar.

Rang | Kandidat (Commits) | Inhalt (geprüft) | Wichtigkeit | Durchführbarkeit | Risiko bei Übernahme | Verdikt
|---:|---|---|---|---|---|---
| 1 | **Lese-Robustheit master.db** `dbf5548` (+ 28 Zeilen Test) | Dort: `new Database(..., { readonly: true, fileMustExist: true, timeout: 10000 })`, `db.pragma('query_only = ON')` und `classifyDbOpenError()` (UNREADABLE u. a.). Hier: `readonly: true` ist vorhanden, **`timeout`, `query_only` und die Fehlerklassifikation fehlen** (Treffer: 0). Folge: ist die DB im Betrieb gesperrt, bricht der Import ohne differenzierte Meldung ab. | **hoch** – betrifft jeden DB-Import, wenn Rekordbox parallel offen hat | **sehr gut** – 4 Zeilen plus ein Testfall; Cherry-Pick konfliktet nur am umgebenden Text (2 Dateien) | **niedrig** – reine Lesehärtung, keine Schreibpfad-Änderung | **sofort übernehmen** (bestes Preis-Leistungs-Verhältnis)
| 2 | **Exakte DB-Ziele für Rekordbox-7-Bibliothek** `2e6a186`, `348aafa`, `66d6d81`, `1b60b8e`, `8572750` | Dort: `queryDbForExactPaths()` + `seedAnlzIndexFromDb()` + UI-Nachfrage nach genauen Pfaden + 274-Zeilen-E2E-Beweis. Hier: `resolveAnalysisFilePath()` übernimmt absolute Pfade unverändert und löst das klassische `/Pioneer/USBANLZ/…`-Muster – **die Bibliotheksform `contents_4136090260/…` liefert `null`** (nachgemessen, §5 D). Der dazugehörige E2E-Test lässt sich hier nicht ausführen: `SyntaxError: … does not provide an export named 'queryDbForExactPaths'`. | **sehr hoch** – genau der Fehler aus dem Diagnosebericht v0.5.6; ohne Lösung kein ANLZ-Zugriff bei katalogbasierten Anlagen | **mittel** – 3 Dateien konfliktieren (App, Resolver, dessen Test); ihr neuer Zweig-Code (`analysisResolver` +~60 Zeilen) und ihr E2E-Test sind übernehmbar, App-Teil muss per Hand | **mittel** – greift in den Auflösungspfad ein; durch ihren E2E-Test + unsere 40 bestehenden Resolver-/DB-Tests gut abfedbar | **übernehmen, Test zuerst**: E2E-Test übernehmen, scheitern lassen, Funktion ergänzen, bis er grün ist
| 3 | **PPTH-Scan bis in die Oberfläche** `9ea9dad` (Async + Cache + Fortschritt) | Kernfunktion ist **hier schon vorhanden und getestet**: `scanAnlzForPaths`/`scanAnlzForPathsAsync(…, onProgress)` in `electron/dbReader.cjs` mit Tiefen-/Mengenbudget, Symlink-Sperre, Header-only-Lesung, `tests/anlz-ppth-scan.test.mjs`. Aber: **kein einziger Verweis in `electron/preload.cjs` (Treffer 0), kein `ipcMain.handle` dafür, `electron/main.cjs` importiert nur 4 Symbole aus dem `dbReader`** → der Scan ist im ausgelieferten Programm nicht erreichbar. Dort ist er verdrahtet (`main.cjs:225`, `preload.cjs` 49 Zeilen, Ereignis `anlz-ppth-scan-progress`). | **mittel-hoch** – ohne Anbindung entweder kein Fortschrittssignal oder Blockade im Main-Prozess; zudem toter, mitgetesteter Code im Auslieferungsbaum | **gut** – nur drei dünne Schichten (IPC-Handler, Preload-API, Abo im `App.tsx`), ~80 Zeilen; Cherry-Pick konfliktiert stark (6/7 Dateien), weil sein App-Teil bei uns anders aussieht | **niedrig-mittel** – neuer IPC-Kanal; Lesezugriff unverändert, Write-Pfad unangetastet | **übernehmen (nur die Verdrahtung)**, nicht ihren App-Teil; dabei den Fortschrittswert im `App.tsx`-Umbruch von B1 mitnehmen
| 4 | **Versions-Schema + Veröffentlichungsskript** `4acc0dc` | Dort: `docs/VERSIONIERUNG.md` (80 Zeilen), `scripts/release.mjs` (115), `tests/version-schema.test.mjs` (127), `package.json`-Skripte. Hier: `src/utils/appVersion.ts` ist **byte-identisch** (Build-Zeit-Injektion `__APP_VERSION__` aus `package.json`) – die Regel „eine Quelle“ gilt bereits; Dokumentation und Schema-Wächter fehlen. | **mittel** – beantwortet Punkt 2 direkt und verhindert den nächsten Drift (0.4.20 vs. v0.5.10) | **sehr gut** – 3 Dateien konfliktfrei übernehmbar; nur `package.json` (2 Skripteinträge) per Hand; ihr `waveform-gates`-Anteil entfällt, Regel stattdessen in `tests/` als `.mjs`-Suite (2 Dateien konfliktieren, müssen nicht) | **niedrig** – keine Laufzeitwirkung | **übernehmen**, unmittelbar vor der nächsten Versionsvergabe, damit der Wächter den ersten bump prüft
| 5 | **Export-/Originalschutz-Agent** `7030599` (+ `996b915`, `3b87188`) | Dort: dauerhafter Wächter `electron/originalGuard.cjs`, `src/agent/originalProtectionAgent.ts`, `OriginalProtectionModal.tsx` (127), ExportModal-Anbindung, +500 Zeilen Test. Hier: die **harte** Regel existiert bereits – `electron/pathGuard.cjs` mit `isProtectedTarget()` (im Main-Prozess bei jedem Zielpfad) plus `tests/path-guard.test.mjs`; **Erklärung, Angebot eines Ersatzziels und dauerhaftes Monitoring existieren nicht**. | **mittel** – Regelwirkung ist gleich, aber die Rückmeldung beim Nutzer fehlt | **mittel** – 6 Dateien konfliktieren, darunter `App.tsx` und `ExportModal.tsx` (beide in B1 in Bewegung) | **niedrig-mittel** – zusätzliche UI-Oberfläche braucht deutsche Texte und Tests | **nach B1 übernehmen**; Vorgriff möglich: nur die ExportModal-Seite (19 Zeilen) vorziehen
| 6 | **Gate-Berichtswesen** `e20ffeb`, `005a4f3`, `b840c11`, `f6d7953`, `8cb0be5` | Dort: `scripts/waveform-gates.mjs` (294), `scripts/gate-report.mjs` (159) mit JSON-Bericht, einem Auto-Retry und Eskalation, plus CI-Artefakt. Hier: **kein Gate-Skript**, dafür `npm test` (28 Suiten, 226 Vitest-Fälle), `npm run coverage` als Schwelle, `tests/source-hygiene.test.ts` (verbotene Muster), `tests/test-registry.test.ts` (Ratchet) und im CI ein Prüfbericht mit `SHA256SUMS.txt`. | **mittel** – der JSON-Bericht ist für Automation nützlich, die Prüftiefe haben wir bereits | **mittel** – ihr Gate liest ihre eigene Fixture-Struktur und ihren Parser; 1:1-Übernahme wäre Rückfall auf schwächere Regeln | **mittel-hoch bei 1:1-Übernahme** – ihr Gate würde „grün“ melden nach seinen Maßstäben, nicht nach „nur echte ANLZ-Werte“ → trügerisches Qualitätssignal | **Idee ja, Code nein**: JSON-Bericht + Auto-Retry + CI-Artefakt auf **unsere** Suiten anwenden; ihren `waveform-gates.mjs` nicht übernehmen
| 7 | **Mix Lab** `af16487`, `05f5c80`, `535b95a` | Drei komplett fehlende Module (`src/mixlab/mixPreviewModel.ts` 481, `mixPreviewPlayer.ts` 163, `MixLabView.tsx` 832) + `tests/mix-preview.test.ts` (455) + Menüpunkt. Hier: keine Spur (0 Dateien). | **niedrig-mittel** – neues Feature, kein Fehlerbezug | **mechanisch gut** (nur 3 von 8 Dateien konfliktieren), **verifikatorisch teuer**: ein dritter Wiedergabepfad müsste in Herkunftsnachweis und Edit-Kombinationsbeweis einbezogen werden | **hoch** – Misch-Vorschau ohne Herkunftskennzeichnung wäre genau die Lücke, die wir in Aufgabe 3 geschlossen haben | **zurückstellen** (nach B1/C1, als eigenes Vorhaben mit eigenem Herkunftsnachweis)
| 8 | **Suche erzwungen auf Partition D:** `274c89e`, `a9ced27`, `a7a2391`, `1a37880`, `2d7cef5` | Dort hartkodiert: `D:\Pioneer`, `D:\Pioneer\ANLZ`, `D:\Pioneer\USBANLZ`, `D:\Rekordbox`, `D:\rekordbox`, `D:\Rekordbox\share\PIONEER\USBANLZ`. Hier: **keine** Laufwerkswurzel hartkodiert; Auflösung arbeitet über die von DB/XML erklärten Pfade (nachgemessen: absolut → unverändert übernommen). | **Entscheidung, kein Bug** – die Vorgabe „nur D:“ kam am 2026-09-10 mündlich; unsre Variante ist anwendungsoffener, aber nicht wörtlich konform | **gut, aber sinnwidrig** – Code wäre in ~10 Zeilen gesetzt | **hoch bei Übernahme** – Rekordbox legt `master.db` standardmäßig in `%APPDATA%` (C:) an; die Erzwingung würde auf solchen Anlagen den Import **abschalten** | **nicht als Code übernehmen**: Vorgabe als Regel dokumentieren und – wenn gewünscht – ein **konfigurierbares** Wurzelverzeichnis (Vorgabe `D:`) statt Hartkodierung anbieten
| 9 | **CI: SQLCipher für Electron-ABI** `6f5cf65`, `87d4403`, `1e90f8b`, `5306f34` | Beide bauen selbst (`npm run rebuild:electron`). Ihr Zusatz: Warnung `::warning::` und Weiterbau **ohne** DB-Import, wenn das optionale Modul fehlt; `npmRebuild: false` ist **hier bereits gesetzt** (gemessen). Mein Schnitt: Build bricht **rot** ab → ein grüner Build garantiert den DB-Import. | **niedrig** – nur CI-Strategie | – | **hoch bei Übernahme** – ein grüner Build ohne DB-Import war der Ausgangsfehler, den die Linie später selbst korrigieren musste | **nicht übernehmen** (unsre Regel ist bewusster strenger)
| 10 | **Übernimmt sich, weil schon hier** – geprüft und verworfen | `3e10029` Datei-Log → `electron/logWriter.cjs` + `src/utils/fileLog.ts` vorhanden · `53c0bfa` Palette-Ziehen → `src/dnd/dragPayload.ts` + `tests/edit-dnd.test.ts` · `07221fc`/`d6b9277` EXT-Schwester → `src/rekordbox/analysisContainerLoader.ts` + `tests/anlz-ext-merge.test.ts` · `7212027`/`40b1cee`/`172f75a` ANLZ während Edits → `src/edit/editWaveform.ts` + `src/waveform/preview.ts` + 85 Kombinationsfälle · `86fe1c2` Deck-Ansicht → `ClipDeckView.tsx` vorhanden, `tests/pitch-tempo.test.ts` vorhanden · `9c671a9` Demo-Daten entfernen → bei uns 0 Treffer in `src/` (und `src/audio/synthesizerTrack.ts` existiert in **beiden** Bäumen nicht mehr) · `73000c1`/`925b1cc` `base: './'` + CORS → `vite.config.ts:18` gesetzt · `0e245d7` Programmname → identisch · `ed22fa3` `ensureArrayBuffer` → **nur hier**, dort nie eingeführt | – | – | – | **nichts zu tun** – und `d04e843` („dynamic waveform synthesis“) bleibt dauerhaft ausgeschlossen

**Korrektur zur Sitzungsnotiz:** die dort genannten Commits `460f686`, `7b77768`, `8f02310`, `92548a6` existieren in diesem Repository **nicht** (`git rev-parse` schlägt fehl) und sind in dieser Bewertung nicht berücksichtigt.

## 3. Punkt 2: Version und Tag – drei Wege, einer empfohlen

| Weg | Vorgehen | Folge | Bewertung |
|---|---|---|---|
| A | Bei **0.4.20** bleiben, `v0.5.10` als Fremdetikett stehen lassen | Die ausgelieferte.exe heißt 0.4.20, die Tags sagen 0.5.x → die Verwirrung bleibt | abgelehnt |
| B | **Empfohlen:** PR #14 mergen, dann `package.json` auf **0.5.11** setzen, `docs/VERSIONIERUNG.md` + Schema-Wächter übernehmen, **einen** vereinheitlichenden Tag `v0.5.11` auf den Merge-Commit von `main` setzen und per `git tag -a` die Herkunft dokumentieren („Strang A = v0.4.x/v0.5.10-Zweig, Strang B = diese Linie“). Nicht gemergt wird nichts. | Eine Nummer, eine Quelle, beide Historien bleibend nachvollziehbar; CI-Bau läuft unter der neuen Nummer | **empfohlen** – kein Umschreiben von Tags, keine erzwungene Verwandtschaft |
| C | Tags `v0.5.2…v0.5.10` auf diese Linie umhängen, um „Lücken“ zu schließen | Öffentliche Tags verschwinden/verschieben sich, installierte Varianten (0.4.13, v0.4.20) verlieren ihre Referenz | abgelehnt |

Nebenbefund: `v0.5.1` fehlt im Tag-Bestand, `1a9c880` (Rekordbox-7.2.16-Key-Recherche, +1 auf dem Fremdzweig) ist reine Dokumentation und kann als `docs/`-Datei mit übernommen werden, wenn die Key-Frage wieder auftaucht.

## 4. Punkt 1 und 3: Was jetzt zu tun ist (Reihenfolge mit Gates)

1. **PR #14 mergen** – `MERGEABLE`, CI grün auf drei Läufern, Gegenstand: Abdeckung 90,4 %, sechs Produktfehler behoben, Doku-Ordnung. `tsc`, 28 Suiten (226 Vitest-Fälle), Build und Diff-Prüfung sind erfüllt, Abdeckung 90,4 %. Kein Grund zu warten.
2. **Unmittelbar danach, gleicher Branch oder frischer Folgebranch:** Kandidat 1 (DB-Lesehärtung) + Kandidat 4 (Versions-Schema) als *ein* Commit mit Gates (`tsc`, `npm test`, `npm run build`, `git diff --check`, Coverage-Schwelle) → Version **0.5.11**, Tag nach Weg B.
3. **Dann Kandidat 2** (exakte DB-Ziele) im TDD-Umkehrbeweis: erst ihren E2E-Test übernehmen und scheitern lassen, dann `queryDbForExactPaths`/`seedAnlzIndexFromDb` ergänzen, danach B1 Schritt 2 fortsetzen – der App-Anteil beider Arbeiten liegt in denselben `App.tsx`-Regionen.
4. **Dann Kandidat 3** (Scan-Anbindung) zusammen mit B1/C1.3, weil beides `App.tsx`/`preload`/Render-Takt berührt.
5. **Kandidat 5 nach B1**, Kandidat 6 als Doku-Idee, Kandidat 7 zurückstellen, Kandidat 8 als Regeltext statt Code, Kandidat 9/10 nichts.

## 5. Verifikation – wie jede Zeile entstanden ist

Alle Befehle laufen im Repository; Ausgänge sind in §2 zitiert.

* **A Topologie:** `git rev-list --count origin/main`, `git log -1 --format=%s origin/main`, `git merge-base origin/main v0.5.10; echo exit=$?` (→ leer, Exit 1), `git rev-list --count HEAD..v0.5.10` (81), `git rev-list --count v0.5.10..HEAD` (10), `git tag -l | sort -V`, `git show v0.5.10:package.json | grep '"version"'`.
* **B Feature-Bestand:** Dateidoppelmessung `test -e <pfad>` gegen `git cat-file -e v0.5.10:<pfad>`; Verhaltensmessung per `grep -cE "query_only|busy_timeout" electron/dbReader.cjs` (0) gegenüber `git show v0.5.10:electron/dbReader.cjs | grep -cE …` (2); Anbindungsprüfung `grep -cE "anlz|ppth|scan" electron/preload.cjs` (0) gegenüber `git show v0.5.10:electron/main.cjs | grep -nE "scanAnlz"` (Zeile 225).
* **C Überlappung:** Exportlisten beider `src/rekordbox/analysisResolver.ts` (hier `joinAudioPath`, `resolveVerifiedDbIdentity`; dort `queryDbForExactPaths`, `seedAnlzIndexFromDb`), Dateilisten `git ls-tree -r --name-only` in beide Richtungen.
* **D Funktionstest statt Vermutung:** `resolveAnalysisFilePath('D:\PIONEER\Master', …)` mit fünf Pfadformen ausgeführt – `contents_…`-Formen → `null`, absolute und `/Pioneer/USBANLZ/…`-Formen → aufgelöst.
* **E theirer E2E-Test gegen unseren Stand:** `tests/exact-paths-e2e.test.ts` aus `v0.5.10` hereinkopiert und ausgeführt → Abbruch wegen fehlender Exporte (belegter Stand, nicht nur fehlende Zeile). Datei wurde **nicht** eingecheckt.
* **F Durchführbarkeit:** je Kandidat `git cherry-pick -n` in einem Wegwerf-Worktree (`git worktree add --detach /tmp/cp HEAD`), Konfliktlisten unten, danach `cherry-pick --abort` + `git worktree remove --force` (Arbeitsbaum unverändert, `git status` leer).
* **G Pull-Request-Lage:** `gh pr list --state all` → #14 `OPEN MERGEABLE`, #12 `OPEN CONFLICTING` (36 Dateien, +6448/−269).

Konfliktlage aus F (Ergebnis der Trockenläufe; rechts steht, was konfliktfrei übernehmbar wäre):

| Kandidat | konfliktieren | konfliktfrei |
|---|---|---|
| `dbf5548` | `electron/dbReader.cjs`, `tests/db-reader-keys.test.mjs` | – |
| `4acc0dc` | `BUILD_WINDOWS.md`, `package.json`, `scripts/waveform-gates.mjs` | `docs/VERSIONIERUNG.md`, `scripts/release.mjs`, `tests/version-schema.test.mjs` |
| `9ea9dad` | `electron/dbReader.cjs`, `electron/main.cjs`, `electron/preload.cjs`, `package.json`, `src/App.tsx`, `src/types/desktop.d.ts` | `tests/anlz-scan-async.test.mjs` |
| `2e6a186` | `src/App.tsx`, `src/rekordbox/analysisResolver.ts`, `tests/analysis-resolver.test.ts` | – |
| `1b60b8e` | `package.json`, `src/rekordbox/analysisResolver.ts` | `tests/exact-paths-e2e.test.ts` |
| `7030599` | `docs/ROADMAP_0.4.20_PLUS.md`, `package.json`, `scripts/waveform-gates.mjs`, `src/App.tsx`, `src/components/MenuBar.tsx`, `src/components/Modals/ExportModal.tsx` | `electron/originalGuard.cjs`, `src/agent/originalProtectionAgent.ts`, `src/components/OriginalProtectionModal.tsx`, `tests/original-*.test.*` |
| `af16487` | `docs/ROADMAP_0.4.20_PLUS.md`, `package.json`, `src/App.tsx` | `src/mixlab/*`, `src/components/MixLabView.tsx`, `tests/mix-preview.test.ts` |

## 6. Grenzen dieser Prüfung (bewusst offen gelassen)

* Es wurde **nur statisch und in Trockenläufen** gemessen; der Code der anderen Linie wurde hier nicht ausgeführt, nicht auf Windows gebaut und nicht an einer echten Rekordbox-Anlage getestet. „Übernehmbar“ heißt also: konfliktarm übertragbar, nicht lauffähig nachgewiesen.
* Pfadformen in D) sind nachgestellte Beispielwerte aus dem Diagnosebericht, keine Kopie einer echten Anlage.
* Der Zahlenstand „0 Treffer“ heißt: kein Beleg im durchsuchten Pfad – nicht die Behauptung, dass es die Fähigkeit nirgends gibt (Suchmuster in §5 B sind die Einschränkung).
* `docs/ROADMAP_0.4.20_PLUS.md` (deren Produktplan) wurde nur gelesen, nicht übernommen: seine Positionierung ist deckungsgleich mit `docs/PROJEKTANALYSE_2026-09-12.md`, sein Aufwandsteil ist durch unsere Roadmap B1–C2 überholt.
